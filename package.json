{
  "name": "flipclick",
  "version": "1.0.0",
  "description": "FlipClick - Convert PDF, PNG, JPG with high-quality editing tools",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "pdf-lib": "^1.17.1",
    "pdf-parse": "^1.1.1",
    "sharp": "^0.32.0",
    "pdfkit": "^0.13.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "compression": "^1.7.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}